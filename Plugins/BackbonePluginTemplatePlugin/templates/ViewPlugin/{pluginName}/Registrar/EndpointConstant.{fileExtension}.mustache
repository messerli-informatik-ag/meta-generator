using System.Net.Http;
using Messerli.Backbone.PluginAbstractions.Http.Routing;

namespace Messerli.{{PluginName}}.Registrar
{
    internal static class EndpointConstant
    {
        public const string HelloWorldRoute = "/hello-world";

        public static readonly Endpoint HelloWorldPage = new Endpoint(
            HttpMethod.Get,
            HelloWorldRoute);
    }
}
