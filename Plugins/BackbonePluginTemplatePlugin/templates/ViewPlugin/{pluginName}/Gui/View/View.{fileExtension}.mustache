using System.Threading.Tasks;
using Messerli.Backbone.PluginAbstractions.Http;
using Messerli.Backbone.PluginAbstractions.PageRendering;
using static Messerli.{{PluginName}}.Gui.View.ResponseRenderer;

namespace Messerli.{{PluginName}}.Gui.View
{
    public sealed class View : IView
    {
        public const string PluginTemplateName = "PluginTemplate";

        public async Task<Response> ShowPluginTemplate(IPageRenderer renderer, ViewModel viewModel)
            => await RenderResponse(renderer, viewModel.PageTitle, PluginTemplateName, viewModel);
    }
}
